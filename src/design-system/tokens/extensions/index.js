import CORNERS from './corners';
import ITEM_TOKENS from '../../helpers/item/tokens'
import fontVariants from '../../helpers/fontVariants'

export {CORNERS, ITEM_TOKENS, fontVariants};

export default {
  CORNERS,
  ITEM_TOKENS,
  fontVariants
};
