const collapses = () => {
  return {
    variants: {
      default: {
        size: 'sm',
        variant: 'transparent',
        rightIconSizeOverride: 10,
      },
    },
  };
};

export default collapses;
