import boxVariants from './boxVariants';
import fontVariants from './fontVariants';
import compose from './compose';
import MARGINS from './margins';
import PADDINGS from './paddings';

export {boxVariants, fontVariants, compose, MARGINS, PADDINGS};

export default {boxVariants, fontVariants, compose, MARGINS, PADDINGS};
